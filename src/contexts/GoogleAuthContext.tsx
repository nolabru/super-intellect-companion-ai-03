
// This file is now just a re-export of the main GoogleAuthContext provider from the google-auth directory
import { GoogleAuthProvider, useGoogleAuth } from './google-auth/GoogleAuthContext';

export { 
  GoogleAuthProvider, 
  useGoogleAuth 
};
